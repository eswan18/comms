apiVersion: v1
kind: ConfigMap
metadata:
  name: comms-staging-env-config
data:
  ENV: "staging"
  PUBSUB_SUBSCRIPTIONS: "comms-staging-footstrike-sub,comms-staging-identity-sub,comms-staging-asset-sub,comms-staging-haruspex-sub"
  EMAIL_FROM: "staging.noreply@mail.identity.ethanswan.com"
  EMAIL_FROM_OVERRIDES: |
    {"haruspex":"Haruspex <staging.noreply@mail.haruspex.fyi>"}
