apiVersion: apps/v1
kind: Deployment
metadata:
  name: comms
spec:
  template:
    spec:
      serviceAccountName: comms-prod-ksa
      containers:
        - name: comms
          envFrom:
            - configMapRef:
                name: comms-config
            - configMapRef:
                name: comms-prod-env-config
          env:
            - name: RESEND_API_KEY
              valueFrom:
                secretKeyRef:
                  name: comms-prod-secrets
                  key: RESEND_API_KEY
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: comms-prod-secrets
